# Secrets only — everything else is in config.yml

# Discord bot token (Developer Portal → Bot → Reset/Copy Token)
DISCORD_TOKEN=your_bot_token_here

# Command channel ID (Developer Mode → right-click channel → Copy Channel ID)
COMMAND_CHANNEL_ID=123456789012345678
